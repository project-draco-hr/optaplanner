{
  BusHub hub=solution.getHub();
  return new DepotAngleCustomerDifficultyWeight(visit,visit.getLocation().getAngle(hub.getLocation()),visit.getLocation().getDistanceTo(hub.getLocation()) + hub.getLocation().getDistanceTo(visit.getLocation()));
}
