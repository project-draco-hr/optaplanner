{
  BusHub hub=solution.getHub();
  return new DepotAngleCustomerDifficultyWeight(visit,visit.getLocation().getAngle(hub.getLocation()),visit.getLocation().getMaximumDistanceTo(hub.getLocation()) + hub.getLocation().getMaximumDistanceTo(visit.getLocation()));
}
