{
  if (this == o) {
    return true;
  }
 else   if (o instanceof Hop) {
    Hop other=(Hop)o;
    return new EqualsBuilder().append(team,other.team).append(fromTeam,other.fromTeam).append(toTeam,other.toTeam).isEquals();
  }
 else {
    return false;
  }
}
