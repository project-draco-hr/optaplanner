{
  return new CompareToBuilder().append(a.getLocation().getLongitude(),b.getLocation().getLongitude()).append(a.getLocation().getLatitude(),b.getLocation().getLatitude()).append(a.getId(),b.getId()).toComparison();
}
