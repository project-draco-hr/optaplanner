{
  if (this == o) {
    return true;
  }
 else   if (o instanceof ShiftSwitchMove) {
    ShiftSwitchMove other=(ShiftSwitchMove)o;
    return new EqualsBuilder().append(leftEmployeeAssignment,other.leftEmployeeAssignment).append(rightEmployeeAssignment,other.rightEmployeeAssignment).isEquals();
  }
 else {
    return false;
  }
}
