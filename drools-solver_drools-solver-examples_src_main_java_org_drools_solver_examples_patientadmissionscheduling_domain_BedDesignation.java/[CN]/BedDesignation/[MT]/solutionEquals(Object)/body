{
  if (this == o) {
    return true;
  }
 else   if (o instanceof BedDesignation) {
    BedDesignation other=(BedDesignation)o;
    return new EqualsBuilder().append(id,other.id).append(admission,other.admission).append(bed,other.bed).isEquals();
  }
 else {
    return false;
  }
}
