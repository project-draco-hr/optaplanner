{
  if (this == o) {
    return true;
  }
 else   if (o instanceof PillarChangeMove) {
    PillarChangeMove other=(PillarChangeMove)o;
    return new EqualsBuilder().append(variableDescriptor,other.variableDescriptor).append(pillar,other.pillar).append(toPlanningValue,other.toPlanningValue).isEquals();
  }
 else {
    return false;
  }
}
