{
  if (this == o) {
    return true;
  }
 else   if (o instanceof TimeslotChangeMove) {
    TimeslotChangeMove other=(TimeslotChangeMove)o;
    return new EqualsBuilder().append(lesson,other.lesson).append(toTimeslot,other.toTimeslot).isEquals();
  }
 else {
    return false;
  }
}
