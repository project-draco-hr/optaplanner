{
  super.phaseEnded(phaseScope);
  bruteForceEntityWalker.phaseEnded(phaseScope);
  logger.info("Phase ({}) bruteForce ended: step total ({}), time spent ({}), best score ({}).",phaseIndex,phaseScope.getLastCompletedStepScope().getStepIndex() + 1,phaseScope.calculateSolverTimeMillisSpent(),phaseScope.getBestScore());
}
