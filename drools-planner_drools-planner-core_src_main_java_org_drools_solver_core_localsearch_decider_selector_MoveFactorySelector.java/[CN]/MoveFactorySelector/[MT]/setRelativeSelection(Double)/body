{
  this.relativeSelection=relativeSelection;
  if (relativeSelection < 0.0 || relativeSelection > 1.0) {
    throw new IllegalArgumentException("The selector's relativeSelection (" + relativeSelection + ") is not in the range [0.0,1.0].");
  }
}
