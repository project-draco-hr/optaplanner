{
  return new CompareToBuilder().append(betterCount,other.betterCount).append(equalCount,other.equalCount).append(lowerCount,other.lowerCount).append(solverBenchmarkResult,other.solverBenchmarkResult,totalScoreSolverRankingComparator).toComparison();
}
