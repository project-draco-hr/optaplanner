{
  if (benchmarkRankerClass != null) {
    try {
      return benchmarkRankerClass.newInstance();
    }
 catch (    InstantiationException e) {
      throw new IllegalArgumentException("solverBenchmarkRankerClass (" + benchmarkRankerClass.getName() + ") does not have a public no-arg constructor",e);
    }
catch (    IllegalAccessException e) {
      throw new IllegalArgumentException("solverBenchmarkRankerClass (" + benchmarkRankerClass.getName() + ") does not have a public no-arg constructor",e);
    }
  }
  return null;
}
