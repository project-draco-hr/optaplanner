{
  if (this == o) {
    return true;
  }
 else   if (o instanceof RoomChangeMove) {
    RoomChangeMove other=(RoomChangeMove)o;
    return new EqualsBuilder().append(lecture,other.lecture).append(toRoom,other.toRoom).isEquals();
  }
 else {
    return false;
  }
}
