{
  selectedCount=0;
  maxScoreAcceptedList=new ArrayList<GreedyMoveScope>(1024);
  maxScore=greedyStepScope.getSolverPhaseScope().getScoreDefinition().getPerfectMinimumScore();
  earlyPickedMoveScope=null;
}
