{
  if (this == o) {
    return true;
  }
 else   if (o instanceof LectureSwitchMove) {
    LectureSwitchMove other=(LectureSwitchMove)o;
    return new EqualsBuilder().append(leftLecture,other.leftLecture).append(rightLecture,other.rightLecture).isEquals();
  }
 else {
    return false;
  }
}
