{
  if (index < 0L || index >= getSize()) {
    throw new IndexOutOfBoundsException("The index (" + index + ") must < from ("+ from+ ") and >= to ("+ to+ ").");
  }
  return incrementUnit.multiply(BigDecimal.valueOf(index)).add(from);
}
