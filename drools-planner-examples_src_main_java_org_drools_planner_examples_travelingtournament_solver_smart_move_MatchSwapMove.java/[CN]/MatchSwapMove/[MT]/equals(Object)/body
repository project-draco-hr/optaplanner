{
  if (this == o) {
    return true;
  }
 else   if (o instanceof MatchSwapMove) {
    MatchSwapMove other=(MatchSwapMove)o;
    return new EqualsBuilder().append(leftMatch,other.leftMatch).append(rightMatch,other.rightMatch).isEquals();
  }
 else {
    return false;
  }
}
