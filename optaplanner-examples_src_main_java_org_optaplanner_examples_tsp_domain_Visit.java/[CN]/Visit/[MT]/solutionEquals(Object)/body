{
  if (this == o) {
    return true;
  }
 else   if (o instanceof Visit) {
    Visit other=(Visit)o;
    return new EqualsBuilder().append(id,other.id).append(city,other.city).append(previousStandstill,other.previousStandstill).isEquals();
  }
 else {
    return false;
  }
}
