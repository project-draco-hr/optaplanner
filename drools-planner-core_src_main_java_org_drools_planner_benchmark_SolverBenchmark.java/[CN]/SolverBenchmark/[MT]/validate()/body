{
  if (unsolvedSolutionFileList == null || unsolvedSolutionFileList.isEmpty()) {
    throw new IllegalArgumentException("Configure at least 1 <unsolvedSolutionFile> for the <solverBenchmark> (" + name + ") directly or indirectly by inheriting it.");
  }
}
