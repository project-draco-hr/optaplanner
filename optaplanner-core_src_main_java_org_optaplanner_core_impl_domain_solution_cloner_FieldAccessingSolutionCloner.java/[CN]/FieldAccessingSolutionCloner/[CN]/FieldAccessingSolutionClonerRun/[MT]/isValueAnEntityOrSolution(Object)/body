{
  Class valueClass=originalValue.getClass();
  if (solutionDescriptor.hasEntityDescriptor(valueClass) || valueClass == ((Class)solutionDescriptor.getSolutionClass())) {
    return true;
  }
  return false;
}
