{
  List<Object> entities=new ArrayList<Object>(leftPlanningEntityList.size() + rightPlanningEntityList.size());
  entities.addAll(leftPlanningEntityList);
  entities.addAll(rightPlanningEntityList);
  return entities;
}
