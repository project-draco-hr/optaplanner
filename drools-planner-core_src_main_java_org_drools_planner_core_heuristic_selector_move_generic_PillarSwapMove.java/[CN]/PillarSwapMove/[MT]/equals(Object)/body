{
  if (this == o) {
    return true;
  }
 else   if (o instanceof PillarSwapMove) {
    PillarSwapMove other=(PillarSwapMove)o;
    return new EqualsBuilder().append(variableDescriptors,other.variableDescriptors).append(leftEntityList,other.leftEntityList).append(rightEntityList,other.rightEntityList).isEquals();
  }
 else {
    return false;
  }
}
