{
  IncrementalScoreCalculator<Solution_> incrementalScoreCalculator=ConfigUtils.newInstance(this,"incrementalScoreCalculatorClass",incrementalScoreCalculatorClass);
  return new IncrementalScoreDirector<>(this,constraintMatchEnabledPreference,incrementalScoreCalculator);
}
