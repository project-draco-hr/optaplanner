{
  if (this == o) {
    return true;
  }
 else   if (o instanceof SeatDesignationSwapMove) {
    SeatDesignationSwapMove other=(SeatDesignationSwapMove)o;
    return new EqualsBuilder().append(leftSeatDesignation,other.leftSeatDesignation).append(rightSeatDesignation,other.rightSeatDesignation).isEquals();
  }
 else {
    return false;
  }
}
