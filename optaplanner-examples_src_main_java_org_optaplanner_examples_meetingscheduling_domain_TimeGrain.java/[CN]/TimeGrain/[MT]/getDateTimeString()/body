{
  int hourOfDay=startingMinuteOfDay / 60;
  int minuteOfHour=startingMinuteOfDay % 60;
  return dayOfYear + "-JAN " + (hourOfDay < 10 ? "0" : "")+ hourOfDay+ ":"+ (minuteOfHour < 10 ? "0" : "")+ minuteOfHour;
}
