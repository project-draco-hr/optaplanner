{
  if (this == o) {
    return true;
  }
 else   if (o instanceof Journey) {
    Journey other=(Journey)o;
    return new EqualsBuilder().append(id,other.id).append(city,other.city).append(previousTerminal,other.previousTerminal).isEquals();
  }
 else {
    return false;
  }
}
