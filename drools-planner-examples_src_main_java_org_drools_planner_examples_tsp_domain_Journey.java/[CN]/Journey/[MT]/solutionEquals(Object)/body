{
  if (this == o) {
    return true;
  }
 else   if (o instanceof Journey) {
    Journey other=(Journey)o;
    return new EqualsBuilder().append(id,other.id).append(city,other.city).append(previousJourney,other.previousJourney).append(nextJourney,other.nextJourney).isEquals();
  }
 else {
    return false;
  }
}
