{
  if (this == o) {
    return true;
  }
 else   if (o instanceof EmployeeChangeMove) {
    EmployeeChangeMove other=(EmployeeChangeMove)o;
    return new EqualsBuilder().append(assignment,other.assignment).append(toEmployee,other.toEmployee).isEquals();
  }
 else {
    return false;
  }
}
