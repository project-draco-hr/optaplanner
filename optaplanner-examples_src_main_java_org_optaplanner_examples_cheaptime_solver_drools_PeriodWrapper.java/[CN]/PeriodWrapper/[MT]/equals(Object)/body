{
  if (this == o) {
    return true;
  }
 else   if (o instanceof PeriodWrapper) {
    PeriodWrapper other=(PeriodWrapper)o;
    return new EqualsBuilder().append(period,other.period).isEquals();
  }
 else {
    return false;
  }
}
