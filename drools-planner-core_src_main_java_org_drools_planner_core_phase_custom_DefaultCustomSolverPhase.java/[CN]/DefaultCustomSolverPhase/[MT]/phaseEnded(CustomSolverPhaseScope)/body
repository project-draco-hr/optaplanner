{
  super.phaseEnded(phaseScope);
  logger.info("Phase custom ended: step total ({}), time spend ({}), best score ({}).",phaseScope.getLastCompletedStepScope().getStepIndex() + 1,phaseScope.calculateSolverTimeMillisSpend(),phaseScope.getBestScore());
}
