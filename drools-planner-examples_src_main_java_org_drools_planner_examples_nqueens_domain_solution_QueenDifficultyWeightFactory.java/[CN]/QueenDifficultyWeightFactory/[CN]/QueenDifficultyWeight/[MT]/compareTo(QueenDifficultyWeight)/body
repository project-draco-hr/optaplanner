{
  return new CompareToBuilder().append(distanceFromMiddle,other.distanceFromMiddle).append(queen.getColumnIndex(),other.queen.getColumnIndex()).append(queen.getId(),other.queen.getId()).toComparison();
}
