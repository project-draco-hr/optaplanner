{
  String[] lineTokens=splitBySpace(line);
  if (lineTokens.length != numberOfTokens) {
    throw new IllegalArgumentException("Read line (" + line + ") is expected to contain "+ numberOfTokens+ " tokens separated by a space ( ).");
  }
  return lineTokens;
}
