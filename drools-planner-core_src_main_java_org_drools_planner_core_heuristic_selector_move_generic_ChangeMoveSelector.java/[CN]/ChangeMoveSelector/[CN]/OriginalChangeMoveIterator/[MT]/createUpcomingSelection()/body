{
  while (!valueIterator.hasNext(upcomingEntity)) {
    if (!entityIterator.hasNext()) {
      upcomingSelection=null;
      return;
    }
    upcomingEntity=entityIterator.next();
    valueIterator=valueSelector.iterator();
  }
  Object toValue=valueIterator.next(upcomingEntity);
  upcomingSelection=chained ? new ChainedChangeMove(upcomingEntity,valueSelector.getVariableDescriptor(),toValue) : new ChangeMove(upcomingEntity,valueSelector.getVariableDescriptor(),toValue);
}
