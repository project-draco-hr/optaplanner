{
  Shift aShift=a.getShift();
  Shift bShift=b.getShift();
  return new CompareToBuilder().append(bShift.getShiftDate(),aShift.getShiftDate()).append(bShift.getShiftType(),aShift.getShiftType()).append(aShift.getRequiredEmployeeSize(),bShift.getRequiredEmployeeSize()).toComparison();
}
