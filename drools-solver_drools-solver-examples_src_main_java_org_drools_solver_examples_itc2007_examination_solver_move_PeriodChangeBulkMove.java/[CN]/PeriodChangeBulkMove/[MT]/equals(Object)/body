{
  if (this == o) {
    return true;
  }
 else   if (o instanceof PeriodChangeBulkMove) {
    PeriodChangeBulkMove other=(PeriodChangeBulkMove)o;
    return new EqualsBuilder().append(exams,other.exams).append(toPeriod,other.toPeriod).isEquals();
  }
 else {
    return false;
  }
}
