{
  return new CompareToBuilder().append(a.getPriority(),b.getPriority()).append(a.getTaskType().getRequiredSkillList().size(),b.getTaskType().getRequiredSkillList().size()).append(a.getTaskType().getBaseDuration(),b.getTaskType().getBaseDuration()).append(a.getId(),b.getId()).toComparison();
}
