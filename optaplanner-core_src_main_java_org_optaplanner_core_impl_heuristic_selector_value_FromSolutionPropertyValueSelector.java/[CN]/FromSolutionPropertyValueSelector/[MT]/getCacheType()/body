{
  SelectionCacheType inheritCacheType=valueRangeMightContainEntity ? SelectionCacheType.STEP : SelectionCacheType.PHASE;
  return (inheritCacheType.compareTo(minimumCacheType) > 0) ? inheritCacheType : minimumCacheType;
}
