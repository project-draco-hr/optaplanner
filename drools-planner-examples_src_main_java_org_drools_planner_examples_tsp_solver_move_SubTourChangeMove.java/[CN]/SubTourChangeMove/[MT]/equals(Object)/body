{
  if (this == o) {
    return true;
  }
 else   if (o instanceof SubTourChangeMove) {
    SubTourChangeMove other=(SubTourChangeMove)o;
    return new EqualsBuilder().append(startJourney,other.startJourney).append(endJourney,other.endJourney).append(toAfterJourney,other.toAfterJourney).isEquals();
  }
 else {
    return false;
  }
}
