{
  BigDecimal nanos;
  if (token.endsWith("%")) {
    nanos=new BigDecimal(token.substring(0,token.length() - 1)).multiply(new BigDecimal(10000000L));
  }
 else {
    nanos=new BigDecimal(token).multiply(new BigDecimal(1000000000L));
  }
  return nanos.longValueExact();
}
