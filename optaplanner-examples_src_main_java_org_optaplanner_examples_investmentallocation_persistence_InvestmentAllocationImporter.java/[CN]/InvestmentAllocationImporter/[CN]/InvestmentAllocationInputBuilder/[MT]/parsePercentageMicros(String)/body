{
  BigDecimal micros;
  if (token.endsWith("%")) {
    micros=new BigDecimal(token.substring(0,token.length() - 1)).multiply(new BigDecimal(10000L));
  }
 else {
    micros=new BigDecimal(token).multiply(new BigDecimal(1000000L));
  }
  return micros.longValueExact();
}
