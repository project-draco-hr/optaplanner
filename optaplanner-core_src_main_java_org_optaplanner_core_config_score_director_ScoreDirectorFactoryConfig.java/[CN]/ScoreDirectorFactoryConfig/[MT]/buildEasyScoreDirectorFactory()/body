{
  if (easyScoreCalculatorClass != null) {
    EasyScoreCalculator<Solution_> easyScoreCalculator=ConfigUtils.newInstance(this,"easyScoreCalculatorClass",easyScoreCalculatorClass);
    return new EasyScoreDirectorFactory<>(easyScoreCalculator);
  }
 else {
    return null;
  }
}
