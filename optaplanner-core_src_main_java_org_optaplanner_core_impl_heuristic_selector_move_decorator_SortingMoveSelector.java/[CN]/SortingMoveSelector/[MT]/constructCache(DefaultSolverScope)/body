{
  super.constructCache(solverScope);
  sorter.sort(solverScope.getScoreDirector(),cachedMoveList);
  logger.trace("    Sorted cachedMoveList with size ({}) in moveSelector({}).",cachedMoveList.size(),this);
}
