{
  boolean firstToken=true;
  for (  String token : line) {
    if (firstToken) {
      firstToken=false;
    }
 else {
      writer.append(",");
    }
    writer.append(token);
  }
  writer.append("\n");
}
