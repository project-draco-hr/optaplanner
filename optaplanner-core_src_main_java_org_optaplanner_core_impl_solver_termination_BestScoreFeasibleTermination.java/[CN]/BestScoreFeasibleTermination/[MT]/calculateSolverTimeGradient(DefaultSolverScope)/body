{
  FeasibilityScoreDefinition scoreDefinition=(FeasibilityScoreDefinition)solverScope.getScoreDefinition();
  return calculateFeasibilityTimeGradient((FeasibilityScore)solverScope.getStartingInitializedScore(),(FeasibilityScore)solverScope.getBestScore());
}
