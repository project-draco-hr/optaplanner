{
  FeasibilityScoreDefinition scoreDefinition=(FeasibilityScoreDefinition)phaseScope.getScoreDefinition();
  return scoreDefinition.calculateFeasibilityTimeGradient((FeasibilityScore)phaseScope.getStartingScore(),(FeasibilityScore)phaseScope.getBestScore());
}
