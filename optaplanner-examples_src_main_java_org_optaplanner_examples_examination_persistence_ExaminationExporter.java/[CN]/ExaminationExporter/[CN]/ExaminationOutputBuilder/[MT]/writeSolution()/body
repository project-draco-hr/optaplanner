{
  Collections.sort(solution.getExamList(),new PersistableIdComparator());
  for (  Exam exam : solution.getExamList()) {
    bufferedWriter.write(exam.getPeriod().getId() + ", " + exam.getRoom().getId()+ "\r\n");
  }
}
