{
  InputStream in=getClass().getResourceAsStream(solverConfigResource);
  if (in == null) {
    throw new IllegalArgumentException("Reading the solverConfigResource (" + solverConfigResource + ") failed.");
  }
  return configure(in);
}
