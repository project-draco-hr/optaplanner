{
  for (  Day day : travelingTournament.getDayList()) {
    teamsPanel.addRowHeader(HEADER_COLUMN,day,createHeaderPanel(new JLabel(day.getLabel())));
  }
  teamsPanel.addRowHeader(HEADER_COLUMN,null,createHeaderPanel(new JLabel("Unassigned")));
}
