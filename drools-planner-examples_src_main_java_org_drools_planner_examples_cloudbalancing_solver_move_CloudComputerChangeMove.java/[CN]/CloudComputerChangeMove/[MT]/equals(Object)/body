{
  if (this == o) {
    return true;
  }
 else   if (o instanceof CloudComputerChangeMove) {
    CloudComputerChangeMove other=(CloudComputerChangeMove)o;
    return new EqualsBuilder().append(cloudProcessAssignment,other.cloudProcessAssignment).append(toCloudComputer,other.toCloudComputer).isEquals();
  }
 else {
    return false;
  }
}
