{
  if (classLoader != null && kieContainer != null) {
    throw new IllegalStateException("The classLoader (" + classLoader + ") and kieContainer ("+ kieContainer+ ") cannot both be configured because the "+ KieContainer.class.getSimpleName()+ " already has a "+ ClassLoader.class.getSimpleName()+ ".");
  }
}
