{
  if (!randomSelection) {
    return new IteratorToValueIteratorBridge(cachedValueList.iterator());
  }
 else {
    return new IteratorToValueIteratorBridge(new CachedListRandomIterator<Object>(cachedValueList,workingRandom));
  }
}
