{
  if (this == o) {
    return true;
  }
 else   if (o instanceof GenericSwapPillarMove) {
    GenericSwapPillarMove other=(GenericSwapPillarMove)o;
    return new EqualsBuilder().append(leftPlanningEntityList,other.leftPlanningEntityList).append(rightPlanningEntityList,other.rightPlanningEntityList).isEquals();
  }
 else {
    return false;
  }
}
