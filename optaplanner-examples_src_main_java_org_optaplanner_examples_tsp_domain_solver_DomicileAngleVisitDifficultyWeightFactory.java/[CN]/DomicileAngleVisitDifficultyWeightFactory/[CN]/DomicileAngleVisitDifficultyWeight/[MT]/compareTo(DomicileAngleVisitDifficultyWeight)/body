{
  return new CompareToBuilder().append(domicileAngle,other.domicileAngle).append(domicileRoundTripDistance,other.domicileRoundTripDistance).append(visit.getId(),other.visit.getId()).toComparison();
}
