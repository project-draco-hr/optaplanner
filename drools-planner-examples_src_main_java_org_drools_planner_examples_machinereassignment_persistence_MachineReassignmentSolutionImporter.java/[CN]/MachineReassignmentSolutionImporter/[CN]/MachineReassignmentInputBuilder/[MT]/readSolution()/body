{
  machineReassignment=new MachineReassignment();
  machineReassignment.setId(0L);
  readResourceList();
  logger.info("MachineReassignment with {} resources.",new Object[]{machineReassignment.getResourceList().size()});
  return machineReassignment;
}
