{
  return new CompareToBuilder().append(nightCount,other.nightCount).append(disallowedCount,other.disallowedCount).append(other.bedDesignation.getAdmissionPart().getFirstNight().getIndex(),bedDesignation.getAdmissionPart().getFirstNight().getIndex()).append(bedDesignation.getId(),other.bedDesignation.getId()).toComparison();
}
