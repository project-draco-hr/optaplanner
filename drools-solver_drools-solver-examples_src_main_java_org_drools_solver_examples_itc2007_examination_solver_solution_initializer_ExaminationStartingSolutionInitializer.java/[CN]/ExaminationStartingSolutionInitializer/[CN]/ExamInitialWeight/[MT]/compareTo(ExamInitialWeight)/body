{
  return new CompareToBuilder().append(other.totalStudentSize,totalStudentSize).append(other.exam.getTopic().getDuration(),exam.getTopic().getDuration()).append(exam.getId(),other.exam.getId()).toComparison();
}
