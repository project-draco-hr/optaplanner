{
  if (this == o) {
    return true;
  }
 else   if (o instanceof CarBlockDesignation) {
    CarBlockDesignation other=(CarBlockDesignation)o;
    return new EqualsBuilder().append(id,other.id).append(carBlock,other.carBlock).append(railPath,other.railPath).isEquals();
  }
 else {
    return false;
  }
}
