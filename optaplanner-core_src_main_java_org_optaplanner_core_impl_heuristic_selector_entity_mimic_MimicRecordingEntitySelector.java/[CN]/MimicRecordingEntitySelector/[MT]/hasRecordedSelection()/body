{
  if (!hasRecordedSelectionCreated) {
    throw new IllegalStateException("Replay must occur after record." + " The childEntitySelector (" + childEntitySelector + ")'s hasNext() has not been called yet. ");
  }
  return hasRecordedSelection;
}
