{
  if (this == o) {
    return true;
  }
 else   if (o instanceof Customer) {
    Customer other=(Customer)o;
    return new EqualsBuilder().append(id,other.id).append(location,other.location).append(previousStandstill,other.previousStandstill).isEquals();
  }
 else {
    return false;
  }
}
