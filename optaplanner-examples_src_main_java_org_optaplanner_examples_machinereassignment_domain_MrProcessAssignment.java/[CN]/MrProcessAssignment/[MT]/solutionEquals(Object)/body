{
  if (this == o) {
    return true;
  }
 else   if (o instanceof MrProcessAssignment) {
    MrProcessAssignment other=(MrProcessAssignment)o;
    return new EqualsBuilder().append(process,other.process).append(machine,other.machine).isEquals();
  }
 else {
    return false;
  }
}
