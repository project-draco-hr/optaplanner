{
  return new CompareToBuilder().append(domicileRoundTripDistance,other.domicileRoundTripDistance).append(visit.getCity().getLatitude(),other.visit.getCity().getLatitude()).append(visit.getId(),other.visit.getId()).toComparison();
}
