{
  if (selectedCountLimit != null) {
    entitySelector=new SelectedCountLimitEntitySelector(entitySelector,resolvedSelectionOrder.toRandomSelectionBoolean(),selectedCountLimit);
  }
  return entitySelector;
}
