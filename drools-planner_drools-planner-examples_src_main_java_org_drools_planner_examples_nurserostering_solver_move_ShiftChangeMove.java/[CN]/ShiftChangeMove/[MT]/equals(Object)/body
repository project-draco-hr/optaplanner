{
  if (this == o) {
    return true;
  }
 else   if (o instanceof ShiftChangeMove) {
    ShiftChangeMove other=(ShiftChangeMove)o;
    return new EqualsBuilder().append(employeeAssignment,other.employeeAssignment).append(toShift,other.toShift).isEquals();
  }
 else {
    return false;
  }
}
