{
  if (originEntitySelectorConfig == null) {
    throw new IllegalArgumentException("The nearbySelectorConfig (" + this + ") is nearby selection"+ " but lacks a nearbyOriginEntitySelector ("+ originEntitySelectorConfig+ ").");
  }
  if (nearEntityNearbyMethodClass == null) {
    throw new IllegalArgumentException("The nearbySelectorConfig (" + this + ") is nearby selection"+ " but lacks a nearEntityNearbyMethodClass ("+ nearEntityNearbyMethodClass+ ").");
  }
  if (resolvedSelectionOrder != SelectionOrder.ORIGINAL && resolvedSelectionOrder != SelectionOrder.RANDOM) {
    throw new IllegalArgumentException("The nearbySelectorConfig (" + this + ") with nearbyOriginEntitySelector ("+ originEntitySelectorConfig+ ") and nearEntityNearbyMethodClass ("+ nearEntityNearbyMethodClass+ ") has a resolvedSelectionOrder ("+ resolvedSelectionOrder+ ") that is not "+ SelectionOrder.ORIGINAL+ " or "+ SelectionOrder.RANDOM+ ".");
  }
  if (resolvedCacheType.isCached()) {
    throw new IllegalArgumentException("The nearbySelectorConfig (" + this + ") with nearbyOriginEntitySelector ("+ originEntitySelectorConfig+ ") and nearEntityNearbyMethodClass ("+ nearEntityNearbyMethodClass+ ") has a resolvedCacheType ("+ resolvedCacheType+ ") that is cached.");
  }
  if (betaDistributionAlpha != null && betaDistributionAlpha < 0.0) {
    throw new IllegalArgumentException("The nearbySelectorConfig (" + this + ")'s betaDistributionAlpha ("+ betaDistributionAlpha+ ") must be positive.");
  }
  if (betaDistributionBeta != null && betaDistributionBeta < 0.0) {
    throw new IllegalArgumentException("The nearbySelectorConfig (" + this + ")'s betaDistributionBeta ("+ betaDistributionBeta+ ") must be positive.");
  }
}
