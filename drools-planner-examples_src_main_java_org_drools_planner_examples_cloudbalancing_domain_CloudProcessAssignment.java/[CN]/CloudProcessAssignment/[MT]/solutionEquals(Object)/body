{
  if (this == o) {
    return true;
  }
 else   if (o instanceof CloudProcessAssignment) {
    CloudProcessAssignment other=(CloudProcessAssignment)o;
    return new EqualsBuilder().append(id,other.id).append(cloudProcess,other.cloudProcess).append(cloudComputer,other.cloudComputer).isEquals();
  }
 else {
    return false;
  }
}
