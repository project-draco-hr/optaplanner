{
  super.constructCache(solverScope);
  sorter.sort(solverScope.getScoreDirector(),cachedEntityList);
  logger.trace("    Sorted cachedEntityList with size ({}) in entitySelector({}).",cachedEntityList.size(),this);
}
