{
  return new CompareToBuilder().append(shortestDistance,other.shortestDistance).append(railPathList.size(),other.railPathList.size()).append(railNode.getId(),other.railNode.getId()).toComparison();
}
