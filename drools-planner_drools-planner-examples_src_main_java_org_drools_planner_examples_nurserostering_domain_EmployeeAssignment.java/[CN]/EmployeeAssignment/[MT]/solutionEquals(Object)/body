{
  if (this == o) {
    return true;
  }
 else   if (o instanceof EmployeeAssignment) {
    EmployeeAssignment other=(EmployeeAssignment)o;
    return new EqualsBuilder().append(id,other.id).append(employee,other.employee).append(shiftDate,other.shiftDate).append(shift,other.shift).isEquals();
  }
 else {
    return false;
  }
}
