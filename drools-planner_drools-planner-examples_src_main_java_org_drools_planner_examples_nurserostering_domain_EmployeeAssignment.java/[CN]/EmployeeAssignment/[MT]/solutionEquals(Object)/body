{
  if (this == o) {
    return true;
  }
 else   if (o instanceof EmployeeAssignment) {
    EmployeeAssignment other=(EmployeeAssignment)o;
    return new EqualsBuilder().append(id,other.id).append(shift,other.shift).append(employee,other.employee).isEquals();
  }
 else {
    return false;
  }
}
