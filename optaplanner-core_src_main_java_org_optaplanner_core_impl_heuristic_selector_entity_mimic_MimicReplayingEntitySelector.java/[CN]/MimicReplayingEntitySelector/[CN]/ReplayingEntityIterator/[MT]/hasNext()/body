{
  if (!hasRecordedSelectionCreated) {
    throw new IllegalStateException("Replay must occur after record." + " The recordingEntitySelector (" + recordingEntitySelector + ")'s hasNext() has not been called yet. ");
  }
  return hasRecordedSelection;
}
