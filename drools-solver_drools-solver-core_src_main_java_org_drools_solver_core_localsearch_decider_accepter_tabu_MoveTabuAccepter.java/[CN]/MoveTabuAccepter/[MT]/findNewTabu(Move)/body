{
  Move tabuMove;
  if (useUndoMoveAsTabuMove) {
    tabuMove=step.createUndoMove(localSearchSolver.getEvaluationHandler().getStatefulSession());
  }
 else {
    tabuMove=step;
  }
  return Collections.singletonList(tabuMove);
}
