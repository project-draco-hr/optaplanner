{
  if (empty == null) {
    empty=false;
    for (    Iterator<Move> moveIterator : moveIteratorList) {
      if (!moveIterator.hasNext()) {
        empty=true;
        break;
      }
    }
  }
  return !empty;
}
