{
  IncrementalScoreDirector<Object> director=new IncrementalScoreDirector<>(mockIncrementalScoreDirectorFactory(),true,mockIncrementalScoreCalculator(false));
  assertFalse(director.isConstraintMatchEnabled());
}
