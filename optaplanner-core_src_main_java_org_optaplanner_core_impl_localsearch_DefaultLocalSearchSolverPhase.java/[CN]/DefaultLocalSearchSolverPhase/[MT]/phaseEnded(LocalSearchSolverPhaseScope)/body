{
  super.phaseEnded(phaseScope);
  decider.phaseEnded(phaseScope);
  logger.info("Phase ({}) localSearch ended: step total ({}), time spent ({}), best score ({}).",phaseIndex,phaseScope.getLastCompletedStepScope().getStepIndex() + 1,phaseScope.calculateSolverTimeMillisSpent(),phaseScope.getBestScore());
}
