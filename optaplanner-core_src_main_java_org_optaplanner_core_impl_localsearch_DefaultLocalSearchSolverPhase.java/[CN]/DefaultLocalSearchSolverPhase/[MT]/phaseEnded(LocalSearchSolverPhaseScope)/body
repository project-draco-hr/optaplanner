{
  super.phaseEnded(phaseScope);
  decider.phaseEnded(phaseScope);
  logger.info("Phase ({}) localSearch ended: step total ({}), time spent ({}), best score ({}).",phaseIndex,phaseScope.getNextStepIndex(),phaseScope.calculateSolverTimeMillisSpent(),phaseScope.getBestScore());
}
