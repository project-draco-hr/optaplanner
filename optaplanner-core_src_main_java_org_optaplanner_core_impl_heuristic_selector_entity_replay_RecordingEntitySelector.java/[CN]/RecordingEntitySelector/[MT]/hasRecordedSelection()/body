{
  if (!hasRecordedSelectionCreated) {
    throw new IllegalStateException("The method hasRecordedSelection() fails on the selector (" + this + ") because it hasNext() hasn't been called yet.");
  }
  return hasRecordedSelection;
}
