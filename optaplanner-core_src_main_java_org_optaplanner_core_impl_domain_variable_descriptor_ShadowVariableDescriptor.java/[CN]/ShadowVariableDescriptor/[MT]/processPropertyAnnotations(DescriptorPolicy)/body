{
  PlanningShadowVariable shadowVariableAnnotation=variablePropertyAccessor.getReadMethod().getAnnotation(PlanningShadowVariable.class);
  processMappedBy(descriptorPolicy,shadowVariableAnnotation);
}
