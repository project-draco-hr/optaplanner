{
  SimpleScore input=SimpleScore.valueOf(123);
  PlannerTestUtils.serializeAndDeserializeWithAll(input,new PlannerTestUtils.OutputAsserter<SimpleScore>(){
    @Override public void assertOutput(    SimpleScore output){
      assertEquals(123,output.getScore());
    }
  }
);
}
