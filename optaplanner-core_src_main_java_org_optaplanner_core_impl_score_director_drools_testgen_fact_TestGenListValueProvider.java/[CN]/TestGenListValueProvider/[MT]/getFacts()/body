{
  ArrayList<TestGenFact> facts=new ArrayList<>();
  for (  Object o : value) {
    TestGenFact fact=existingInstances.get(o);
    if (fact != null) {
      facts.add(fact);
    }
  }
  return facts;
}
