{
  super.phaseEnded(bruteForceSolverPhaseScope);
  bruteForceEntityWalker.phaseEnded(bruteForceSolverPhaseScope);
  logger.info("Brute force phase ended at step index ({}) for best score ({}).",bruteForceSolverPhaseScope.getLastCompletedStepScope().getStepIndex(),bruteForceSolverPhaseScope.getBestScore());
}
