{
  super.phaseEnded(phaseScope);
  bruteForceEntityWalker.phaseEnded(phaseScope);
  logger.info("Phase bruteForce ended: step total ({}), time spend ({}), best score ({}).",phaseScope.getLastCompletedStepScope().getStepIndex() + 1,phaseScope.calculateSolverTimeMillisSpend(),phaseScope.getBestScore());
}
