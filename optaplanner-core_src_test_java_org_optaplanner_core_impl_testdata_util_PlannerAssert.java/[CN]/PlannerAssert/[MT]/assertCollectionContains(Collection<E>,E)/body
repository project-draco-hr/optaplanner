{
  for (int i=0; i < elements.length; i++) {
    if (!collection.contains(elements[i])) {
      fail("The asserted collection (" + collection + ") does not contain expected element ("+ elements[i]+ ")");
    }
  }
}
